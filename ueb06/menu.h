#ifndef MENU_H
#define MENU_H

/*Prints a title while listing the array with options to create a menu-screen. Returns input/choosen menupoint*/
int getMenu(char *title, char *options[], int optionAmount);

#endif 